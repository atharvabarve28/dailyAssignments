// String handling Operations

// 1. Concatenation with concat() method

const firstName = "Atharva";
const lastName = "Barve";

const fullName = firstName.concat(" ", lastName);

// console.log(fullName);

// 2. Slicing with slice() method

const message = "Atharva, Barve";

const sliceString = message.slice(0, 7);

// console.log(sliceString);

// 3. Substring with substring() method

const message1 = "Atharva, Barve";

const sliceString1 = message1.substring(0, 7)

// console.log(sliceString1);

// 4. Substr with substr() method

const message2 = "Atharva, Barve";

const sliceString2 = message2.substr(0, 7);

// console.log(sliceString2);

